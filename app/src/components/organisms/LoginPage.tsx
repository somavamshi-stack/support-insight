import { LoginPageLayout } from "@templates";

export const LoginPage = () => {
    return (
        <LoginPageLayout>
            {/* SuperTokens will automatically render its prebuilt UI here */}
            <div>Loading authentication...</div>
        </LoginPageLayout>
    );
};

export default LoginPage;
